import type { Request, Response } from "express";
import type { asyncHandler } from "../../utils/asynce.handler";

export const signup = asyncHandler(async (req: Request, res: Response) => {
  const user = 
});
